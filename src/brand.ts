/* ============================================================
   Brand — single source of truth. Renaming the product means
   changing this file only.
   ============================================================ */

export const BRAND = 'GAM3BOOK'
/** The character rendered in the accent colour inside the wordmark. */
export const BRAND_ACCENT = '3'
export const BRAND_SUB = 'Diário social do esporte ao vivo'

/** Fan token symbol (SPL, Solana). */
export const TOKEN_SYMBOL = 'G3B'
