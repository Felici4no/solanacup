/* ============================================================
   Brand — single source of truth. Renaming the product means
   changing this file only; domain types stay brand-neutral.
   ============================================================ */

export const APP_NAME = 'GAM3BOOK'
export const APP_TAGLINE = 'O diário social dos esportes ao vivo'

/** Fan token symbol (SPL). */
export const TOKEN_SYMBOL = 'G3B'
